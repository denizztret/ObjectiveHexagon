// HexagonKitUI is filled in at stage 4: bridges to CoreGraphics, SwiftUI and
// UIKit. Until then the target holds this file only, because SwiftPM refuses to
// build a target whose directory has no Swift file at all.
